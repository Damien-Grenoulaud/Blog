<h3>Recherchez un article :</h3>
<%= form_with model: @article, local: true,method: :get, class: "mb-4" do |form| %>
  <div class="flex gap-4">
    <div class="appinput">
      <%= form.label :title, "Titre :"  %><br>
      <%= form.text_field :title, placeholder: "Titre..." %>

      <%= form.label :categorie, "Catégorie :"  %><br>
      <%= form.select :categorie, options_for_select(Article.categories.map {|key, value| [key.titleize, Article.categories.key(value)]}, @article&.categories), include_blank: true %>

      <br>
      <%= form.label :status, "Statut :"  %><br>
      <%= form.select :status, options_for_select(Status.labels), include_blank: true %>

    </div>
    <%= form.submit :Recherchez, class: "link" %>
  </div>
<% end %>